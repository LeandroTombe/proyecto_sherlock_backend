# ========================================
# CONFIGURACIÓN DEL MICROSERVICIO DE AUTENTICACIÓN
# ========================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Edita .env con tus valores reales
# 3. NUNCA subas .env a GitHub (solo .env.example)
# ========================================

# ========================================
# CONFIGURACIÓN DE FLASK
# ========================================
FLASK_APP=main.py
FLASK_ENV=development
FLASK_DEBUG=1

# ========================================
# CLAVE SECRETA JWT (¡OBLIGATORIO!)
# ========================================
# ⚠️  CAMBIA ESTA CLAVE POR UNA ÚNICA Y SEGURA
# ⚠️  NO USES LA CLAVE POR DEFECTO EN PRODUCCIÓN
FLASK_JWT_SECRET_KEY=tu_clave_secreta_super_segura_aqui

# ========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ========================================

# Para desarrollo local (PostgreSQL instalado en tu máquina):
DATABASE_URL=postgresql://postgres:tu_password@localhost:5432/auth_db_local

# Para Docker (descomenta cuando uses docker-compose):
# DATABASE_URL=postgresql://postgres:postgres@db:5432/auth_db

# Para SQLite (alternativa simple para desarrollo):
# DATABASE_URL=sqlite:///auth.db

# ========================================
# CONFIGURACIÓN DE LA APLICACIÓN
# ========================================
PORT=5000

# ========================================
# CONFIGURACIÓN JWT (OPCIONAL)
# ========================================
# Tiempo de expiración del token de acceso (en segundos)
# JWT_ACCESS_TOKEN_EXPIRES=3600

# Tiempo de expiración del token de refresco (en segundos)
# JWT_REFRESH_TOKEN_EXPIRES=2592000

# ========================================
# CONFIGURACIÓN DE SEGURIDAD (OPCIONAL)
# ========================================
# Habilitar lista negra de tokens
# JWT_BLOCKLIST_ENABLED=True

# Tipos de tokens a verificar en la lista negra
# JWT_BLOCKLIST_TOKEN_CHECKS=["access", "refresh"]

# ========================================
# CONFIGURACIÓN DE LOGGING (OPCIONAL)
# ========================================
# Nivel de logging (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# ========================================
# CONFIGURACIÓN DE CORS (OPCIONAL)
# ========================================
# Orígenes permitidos para CORS
# CORS_ORIGINS=["http://localhost:3000", "http://localhost:8080"]

# ========================================
# NOTAS IMPORTANTES
# ========================================
# 
# 1. CAMBIA FLASK_JWT_SECRET_KEY por una clave única y segura
# 2. Ajusta DATABASE_URL según tu entorno (local/Docker)
# 3. En producción, usa variables de entorno del sistema
# 4. Nunca subas archivos .env a control de versiones
# 5. Este archivo (.env.example) SÍ se sube a GitHub